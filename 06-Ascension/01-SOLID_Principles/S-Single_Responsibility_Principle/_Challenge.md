# Trial: SRP — Refactor a Monolithic Invoice System

Refactor a monolithic `InvoiceManager` class that handles: data storage, tax calculation, PDF formatting, and email sending. Split into 4+ single-responsibility classes and create a coordinator.

*Demonstrate that changing the tax rules requires modifying only ONE class.*
