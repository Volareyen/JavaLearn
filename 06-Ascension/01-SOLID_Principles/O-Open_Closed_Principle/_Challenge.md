# Trial: OCP — Extensible Notification System

Build a notification system with `Notifier` interface. Start with `EmailNotifier` and `SMSNotifier`. Then add `SlackNotifier` and `PushNotifier` WITHOUT modifying any existing class. Prove OCP by showing the router/dispatcher code never changes.
